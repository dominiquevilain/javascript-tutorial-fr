describe("pow", function() {

  it("2 élevé à la puissance 3 est 8", function() {
    assert.equal(pow(2, 3), 8);
  });

  it("3 élevé à la puissance 3 est 27", function() {
    assert.equal(pow(3, 3), 27);
  });

});
